apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - ollama.yaml
  - api.yaml
# Image Updater rewrites the newTag of this entry
# when a newer image is pushed to Docker Hub.
images:
  - name: minhnghia2k3/local-k8s-ai-agent
    newTag: 776a2f9
